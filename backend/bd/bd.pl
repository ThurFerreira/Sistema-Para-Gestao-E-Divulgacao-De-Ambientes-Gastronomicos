:- load_files([ chave,
              estado,
              cidade,
              cadastro,
              status,
              nivel,
              boteco,
              video,
              foto,
              personalidade,
              categoria,
              institucional ],
	[ if(not_loaded), silent(true)]
  
  ).